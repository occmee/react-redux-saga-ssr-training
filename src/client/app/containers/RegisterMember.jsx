import {connect} from 'react-redux';
import {bindActionCreators} from 'redux';
import {RegisterMember} from '../components/registers';

export default connect(state => {
  return {
  };
}, dispatch => {
  return {
  };
})(RegisterMember);
